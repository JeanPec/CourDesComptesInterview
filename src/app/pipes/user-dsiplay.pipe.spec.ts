import { UserDsiplayPipe } from './user-display.pipe';

describe('UserDsiplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDsiplayPipe();
    expect(pipe).toBeTruthy();
  });
});
